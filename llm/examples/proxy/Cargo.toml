[package]
name = "proxy_example"
version = "0.1.0"
edition = "2021"

# Standalone crate (not part of parent workspace)
[workspace]

[dependencies]
# Use the local vllora_llm crate
vllora_llm = { path = "../.." }
tokio = { version = "1", features = ["full"] }
tokio-stream = "0.1"